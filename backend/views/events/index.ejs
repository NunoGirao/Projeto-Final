<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eventos</title>
</head>
<body>
    <h1>Lista de Eventos</h1>
    <ul>
        <% events.forEach(event => { %>
            <li>
                <a href="/backoffice/events/show/<%= event._id %>"><%= event.name %></a>
                <form action="/backoffice/events/delete/<%= event._id %>" method="POST" style="display:inline;">
                    <button type="submit">Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
    <a href="/backoffice/events/create">Criar Novo Evento</a>
    <a href="/backoffice">Voltar</a>
</body>
</html>
